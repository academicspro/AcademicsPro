import express from 'express';


const router = express.Router();


router.post('/teacher/attendence', );
router.get('/teacher/attendence', );
router.get('/teacher/attendence/:id',);
router.put('/teacher/attendence/:id',);
router.delete('/teacher/attendence/:id',);


export default router;